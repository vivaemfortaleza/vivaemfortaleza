$("#modal-window").html("<%= escape_javascript(render 'new_horario') %>");
$("#modal-window").modal('show');

var horarios = [];

var proccesor = {
  from: function(json){
    for(var prop in json)
      $("#" + prop).val(json[prop]);
  }
}

$("#add-horario-button").click(function(){
  var horario = {
    inicio : $('#inicio').val(),
    fim : $('#fim').val(),
    horario: $("#horario").val()
  };


});
